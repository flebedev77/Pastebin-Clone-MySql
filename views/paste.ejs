<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= locals.title %>
    </title>
    <style>
        body {
            background: #22245f;
            color: white;
            font-weight: bold;
            font-family: sans-serif;
            margin: 10px;
        }

        h1 {
            width: fit-content;
            max-width: 100vw;
            border-bottom: 2px solid white;
            margin: 0;
            padding: 2px;
        }

        p {
            margin-left: 10px;
            font-weight: normal;
        }

        div {
            display: flex;
            align-items: center;
            gap: 5px;
        }
        span {
            font-size: 12px;
            background: rgba(0, 0, 0, 0.3);
            padding: 5px 10px;
            border-radius: 5px;
            font-weight: lighter;
        }
    </style>
</head>

<body>
    <h1>
        <%= locals.title %>
    </h1>
    <div>
        <p>Uploaded on </p>
        <span><%= locals.date %></span>
    </div>
    <p>
        <%= locals.content %>
    </p>
</body>

</html>